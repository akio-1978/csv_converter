[
{%- for sheet in sheets %}
    {
        "name" : "{{sheet.fixed.C3}}",
        "description" : "{{sheet.fixed.C4}}",
        "lines" : [
    {%- for row in sheet.rows if row.C -%}
            {% if row.C %}
            {
                "date" : "{{row.C | excel_time('%Y/%m/%d')}}",
                "event" : "{{row.D}}",
                "price" : "{{row.H}}"
            }{% if not loop.last %},{%endif -%}
            {%- endif %}
    {%- endfor %}
        ]
    }{% if not loop.last %},{%endif -%}
{%- endfor %}
]
